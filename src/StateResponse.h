#ifndef STATE_RESPONSE_H
#define STATE_RESPONSE_H

// Définition complète de la structure
struct StateResponse {
    uint8_t type;  // Type de la réponse
    bool state;    // État (ON/OFF)
};

#endif // STATE_RESPONSE_H
